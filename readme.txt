# To use this evil webdriver system, you need to install something in the badboy folder
# command as below

$ npm install
$ npm install -g selenium-standalone
$ selenium-standalone install
$ selenium-standalone start

# if the warning "No Java JDK" occurs, download and install it here:
	http://www.oracle.com/technetwork/java/javase/downloads/jdk8-downloads-2133151.html
and then run "selenium-standalone start" again

run 
$ node evil.js